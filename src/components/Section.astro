---
interface Props {
  title: string
  id: string
}

const { id, title } = Astro.props as Props
---

<section {id} class="py-20 container p-2 flex flex-col gap-8">
  <h2 class="text-2xl pl-6">{title}</h2>
  <slot />
</section>
